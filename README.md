# python-quote-generator
A simple Python program that displays a random quote from a text file.

# Random Quote Generator

This project is a simple Python program that reads quotes from a file and
displays one random quote each time it runs.

The goal of this project is to practice basic Python concepts such as file
handling, lists and random selection.
